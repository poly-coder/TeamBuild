<TbMudLoadingBar IsLoading="IsCreating" />

@if (CreateException is not null)
{
    <TbMudExceptionAlert Exception="CreateException" />
}

<AvailableCultureAutocomplete ValueChanged="HandleAvailableCultureChanged" />

<CultureNewFormView OnSubmit="@OnCreate"
                    IsSubmitting="@IsCreating"
                    InitialForm="@initialForm"/>
