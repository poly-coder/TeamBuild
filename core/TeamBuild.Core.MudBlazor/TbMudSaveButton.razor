@using MudBlazor

@inherits TbSaveButton

<TbSaveButton ButtonType="@TbButtonType.Submit"
              Color="@(Color ?? TbColor.Primary)"
              Disabled="@Disabled"
              EndIcon="@EndIcon"
              FullWidth="@FullWidth"
              Href="@Href"
              IconColor="@IconColor"
              Loading="@Loading"
              Rel="@Rel"
              Size="@Size"
              StartIcon="@(StartIcon ?? TbIcon.Save)"
              Target="@Target"
              Variant="@Variant"
              OnClick="@OnClick"
              ChildContent="@ChildContent">
    <LoadingContent>
        <MudProgressCircular Class="ms-n1" Size="global::MudBlazor.Size.Small" Indeterminate="true"/>
        <MudText Class="ms-2">Saving</MudText>
    </LoadingContent>
</TbSaveButton>
