@inherits TbButton

<TbButton ButtonType="@TbButtonType.Submit"
          Color="@(Color ?? TbColor.Primary)"
          Disabled="@Disabled"
          EndIcon="@EndIcon"
          FullWidth="@FullWidth"
          Href="@Href"
          IconColor="@IconColor"
          Loading="@Loading"
          LoadingContent="@LoadingContent"
          Rel="@Rel"
          Size="@Size"
          StartIcon="@(StartIcon ?? TbIcon.Save)"
          Target="@Target"
          Variant="@Variant"
          OnClick="@OnClick">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else{
        <text>Save</text>
    }
</TbButton>
