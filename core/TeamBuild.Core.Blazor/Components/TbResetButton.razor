@inherits TbButton

<TbButton ButtonType="@TbButtonType.Reset"
          Color="@(Color ?? TbColor.Default)"
          Disabled="@Disabled"
          EndIcon="@EndIcon"
          FullWidth="@FullWidth"
          Href="@Href"
          IconColor="@IconColor"
          Loading="@Loading"
          LoadingContent="@LoadingContent"
          Rel="@Rel"
          Size="@Size"
          StartIcon="@(StartIcon ?? TbIcon.Reset)"
          Target="@Target"
          Variant="@Variant"
          OnClick="@OnClick">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else{
        <text>Reset</text>
    }
</TbButton>
